[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rbf_sa_optimizer"
version = "0.1.0"
dependencies = [
    "numpy",
    "scipy",
    "scikit-learn",
]

[tool.setuptools.packages.find]
where = ["src"]
